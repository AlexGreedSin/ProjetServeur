<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tournois Cité</title>

    
    <link rel="stylesheet" href="./css/normalize.css">
    <link rel="icon" href="./img/logocite.jpg">


    {{#each style}}
    <link rel="stylesheet" href="{{this}}">
   {{/each}}

   {{#each scripts}}
   <script type="module" src="{{this}}"></script>
   {{/each}}
   {{#unless accept}}
        <script type="module" src="/js/accept-cookies.js"></script>
    {{/unless}}
    
</head>
<body>
    {{>header}}

    <main>
        {{#unless accept}}
        <div id="message-cookie">
            En naviguant sur ce site web, vous acceptez qu'il utilise des cookies.
            <input type="button" value="Accepter" id="button-cookie">   
        </div>
        {{/unless}}
        {{{body}}}
    </main>
    {{>footer}}
</body>
</html>